using Xunit;

namespace Azure.AI.Vision.Face.DeviceAttestation.Tests;

// Placeholder test that verifies the test project builds and runs against the
// library. Real crypto/handler/attestation tests are added in later phases.
public class ScaffoldingTests
{
    [Fact]
    public void Scaffolding_ProjectBuilds()
    {
        Assert.True(true);
    }
}
